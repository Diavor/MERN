# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
node_modules/
/.pnp
.pnp.js

# testing
/coverage

# production
/frontend/build

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.backup

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local upload store (ephemeral; use object storage in production)
/uploads/*
!/uploads/.gitkeep

# machine-local Claude Code config (permissions, launch profiles)
.claude/settings.local.json
.claude/launch.json

# print-agent: real hardware config is till-machine-specific — never commit it
print-agent/printers.config.json
